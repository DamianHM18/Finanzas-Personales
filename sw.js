// Este es un Service Worker básico para que el navegador reconozca la app como instalable.
self.addEventListener('fetch', (event) => {
    // No hace nada por ahora, pero su existencia es obligatoria para la instalación
});
